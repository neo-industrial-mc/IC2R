# Golden Suite — 行为黄金用例规格

> **状态**：大纲骨架（W0.2）  
> **下一步**：W0.3 填充各节**条目表**（ID / 标题 / 不变量摘要 / 优先级）；用例正文与测试链接可后补。  
> **依据**：[Modernization_Project.md §4.4](../Modernization_Project.md)  
> **索引**：[docs/spec/README.md](README.md)

本文件冻结「必须先规格与测试、再动实现」的模块与用例大纲。条目 ID 建议格式：`{域}-{子域}-{序号}`（例：`EN-IC-001`），测试可用 `@Spec("EN-IC-001")` 对齐（§4.7）。

---

## 0. 元信息

| 项 | 值 |
|:---|:---|
| 规格版本 | 0.1-skeleton |
| 冻结目标 | 章节与填空位稳定，便于 W0.3 直接填表 |
| 覆盖范围 | EnergyNet IC/GT、标准机、配方、NBT/同步；§4.4.6 高风险域预留 |
| 非目标（本文件） | 完整用例正文、实现代码、完整命名审计 |

### 条目表模板（W0.3 起各节复用）

| ID | 标题 | 不变量 / 期望摘要 | 优先级 | 测试状态 | 备注 |
|:---|:---|:---|:---|:---|:---|
| *（W0.3 填入）* | | | P0/P1/P2 | 未写 / 红 / 绿 | |

---

## 1. EnergyNet（IC 模式）

**规格域前缀**：`EN-IC-*`  
**实现对照提示**：现行 IC 路径 / `EnergyCalculatorUnified` 等（以代码检索为准，不在此抄实现）

### 1.1 主题清单（骨架）

| 主题 | 规格要点（摘要，非完整用例） | 条目表 |
|:---|:---|:---|
| 单源单汇 | 路径选择；线损后 EU 到达量 | *W0.3* |
| 多汇分配 | 与现行统一计算器策略对齐的规格描述 | *W0.3* |
| 绝缘 / 导体 | 击穿、熔断、过压爆炸条件 | *W0.3* |
| 变压器 | packet 倍率（升压 / 降压） | *W0.3* |

### 1.2 条目表

> W0.3：在此表填入 `EN-IC-00x` 行；本骨架不抢写完整条目。

| ID | 标题 | 不变量 / 期望摘要 | 优先级 | 测试状态 | 备注 |
|:---|:---|:---|:---|:---|:---|
| | | | | | |

---

## 2. EnergyNet（GT 模式）

**规格域前缀**：`EN-GT-*`  
**外部依据**：[GTEU_Migration_Project.md](../GTEU_Migration_Project.md) invariants；[GTEU_GT_Reference.md](../GTEU_GT_Reference.md)

### 2.1 主题清单（骨架）

| 主题 | 规格要点（摘要，非完整用例） | 条目表 |
|:---|:---|:---|
| 1A 包语义 | 1A 包不可拆分；空闲 1A 请求 | *W0.3* |
| maxAmps | 公式与边界 | *W0.3* |
| 保护 | 超压 / 超流熔断 | *W0.3* |
| 方向 | 方向优先级 | *W0.3* |
| 与 IC 模式隔离 | 双 Calculator / 模式切换不串味（若适用） | *W0.3* |

### 2.2 条目表

| ID | 标题 | 不变量 / 期望摘要 | 优先级 | 测试状态 | 备注 |
|:---|:---|:---|:---|:---|:---|
| | | | | | |

---

## 3. 标准处理机

**规格域前缀**：`SM-*`（Standard Machine）

### 3.1 主题清单（骨架）

| 主题 | 规格要点（摘要，非完整用例） | 条目表 |
|:---|:---|:---|
| 进度与耗电 | 进度推进、EU 消耗、中断 | *W0.3* |
| 阻塞 | 输出满阻塞；输入不足 | *W0.3* |
| 升级 | 超频、变压器、储能、拉取 / 推送 | *W0.3* |
| 配方交互 | 匹配与消耗顺序（与 §4 交叉引用） | *W0.3* |

### 3.2 条目表

| ID | 标题 | 不变量 / 期望摘要 | 优先级 | 测试状态 | 备注 |
|:---|:---|:---|:---|:---|:---|
| | | | | | |

---

## 4. 配方系统

**规格域前缀**：`RC-*`（Recipe）

### 4.1 主题清单（骨架）

| 主题 | 规格要点（摘要，非完整用例） | 条目表 |
|:---|:---|:---|
| 输入匹配 | `IRecipeInput`：item / tag / ore 语义 | *W0.3* |
| 黑白名单 | 回收机等过滤规则 | *W0.3* |
| 序列化 | JSON 反序列化稳定性 | *W0.3* |

### 4.2 条目表

| ID | 标题 | 不变量 / 期望摘要 | 优先级 | 测试状态 | 备注 |
|:---|:---|:---|:---|:---|:---|
| | | | | | |

---

## 5. NBT / 同步

**规格域前缀**：`NS-*`（NBT / Sync）

### 5.1 主题清单（骨架）

| 主题 | 规格要点（摘要，非完整用例） | 条目表 |
|:---|:---|:---|
| 旧键兼容 | 读旧 NBT 键不丢数据 | *W0.3* |
| 新键写出 | 新键 `snake_case` 写出策略 | *W0.3* |
| 同步往返 | 同步字段编解码 round-trip | *W0.3* |

### 5.2 条目表

| ID | 标题 | 不变量 / 期望摘要 | 优先级 | 测试状态 | 备注 |
|:---|:---|:---|:---|:---|:---|
| | | | | | |

---

## 6. 其他高风险域（预留）

> §4.4.6：阶段 0 大纲占位；条目优先级可低于 §1–§5，但结构预留以便后续 Unit 扩展。

**规格域前缀建议**：`RX-*`（Reactor）、`CR-*`（Crop）、`UU-*`（UuGraph）、`FL-*`（Fluid）

### 6.1 主题清单（骨架）

| 主题 | 规格要点（摘要，非完整用例） | 条目表 |
|:---|:---|:---|
| 核电 | 热与爆炸阈值（至少纯函数部分） | 后续 |
| 作物 | 生长关键公式（可测部分） | 后续 |
| UU 图 | `UuGraph` 算法不变量 | 后续 |
| 流体容器 | fill / empty 语义 | 后续 |

### 6.2 条目表

| ID | 标题 | 不变量 / 期望摘要 | 优先级 | 测试状态 | 备注 |
|:---|:---|:---|:---|:---|:---|
| | | | | | |

---

## 7. 追溯与维护

| 动作 | 约定 |
|:---|:---|
| 新增条目 | 分配稳定 ID；补优先级与测试状态 |
| 行为有意变更 | 先改本规格与测试，再改实现；IC2R 差异写明 |
| 实现删除/替换 | 对应条目测试仍绿，或条目标记废弃并说明替代 |
| 与代码对照 | 不在本文件粘贴大段源码；用路径 + 测试名引用 |

---

## 变更记录

| 日期 | 版本 | 说明 |
|:---|:---|:---|
| 2026-07-14 | 0.1-skeleton | W0.2：章节骨架就位，条目表留空待 W0.3 |
